# Prerequisites

node >=0.6.10
npm >=1.1.0-3
selenium-server (http://selenium.googlecode.com/files/selenium-server-standalone-2.25.0.jar)

# Install the dependencies

java -jar "path of selenium-server"
npm install 
npm install -g -force cucumber

# Run the script

cucumber.js feature